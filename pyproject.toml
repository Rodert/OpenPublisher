[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openpublisher"
version = "0.1.0"
description = "AI-native content publishing infrastructure"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

